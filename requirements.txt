# Paper2Code 第一周依赖
# 安装顺序建议：先单独装 vllm（它会带上配套 torch），再装其余。
#   pip install vllm
#   pip install -r requirements.txt

# --- 模型服务 ---
vllm>=0.6.0
openai>=1.40.0            # 调用 vLLM 的 OpenAI 兼容接口

# --- Agent 编排 ---
langgraph>=0.2.0
langchain-core>=0.3.0

# --- 数据校验 ---
pydantic>=2.7.0

# --- 检索（Day 5）---
sentence-transformers>=3.0.0
pypdf>=4.0.0
numpy>=1.26.0

# 注意：torch 不在此处单独列出，由 vllm 自动安装匹配版本，
# 避免手动装 torch 与 vllm 的 CUDA 版本冲突。
